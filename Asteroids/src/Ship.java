/**
 * A class representing a players ship
 * A Ship has:
 * position
 * direction
 * speed
 * velocity
 * graphic
 * weapon
 */
public class Ship {

    /**
     * Ship constructor
     */
    Ship() {
        //set position to middle of the screen
        int gridSize = 1000;
        int x = gridSize / 2;
        int y = gridSize / 2;
        Position shipPosition = new Position(x, y);

        //direction

        //speed

        //velocity

        //graphic

        //weapon
    }
}
